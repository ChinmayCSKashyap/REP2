name: CI  # Name your workflow (use no spaces for badge fixes)

on:
  push:
    branches: [ main ]  # Runs on pushes to main branch
  workflow_dispatch:  # Lets you trigger manually (key for testing)

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run a test
        run: echo "All good! Tests pass."  # Replace with real tests later
